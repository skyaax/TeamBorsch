#include <iostream>
using namespace std;



int main()
{
    int choice;
    int a, b, c;
    cout << "Hello" << endl << "Choose your operation(1.add, 2.subtract, 3.multiply)" << endl;
    cin >> choice;
    if (choice == 1) {
        //add(a,b,c);
    }
    if (choice == 2) {
        //subtract(a,b,c);
    }
    if (choice == 3) {
        //multiply(a,b,c);
    }
}